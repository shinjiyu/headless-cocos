# T04 — Square size 120

In `assets/scripts/HeadlessProbe.ts`, change `SQUARE_SIZE` from `80` to `120`.

Ensure the half-size / rect drawing still match the new size (do not leave hardcoded `40` half if it breaks).
