# T03 — Square fill color to green

In `assets/scripts/HeadlessProbe.ts`, the Graphics square fill color is currently red-ish (`220, 40, 40`).

Change it to a clear green: `Color(40, 200, 80, 255)`.

Keep size and movement logic unchanged.
