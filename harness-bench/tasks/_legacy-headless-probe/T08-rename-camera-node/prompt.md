# T08 — Rename Camera node

Edit `assets/scene/PreviewBoot.scene`.

Rename the node currently named `"Camera"` (under Canvas) to `"MainCamera"`.

Only change that `_name` field. Do not break parent/children references or regenerate UUIDs.
