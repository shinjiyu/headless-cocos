# T06 — Prefab label text

Edit `assets/prefabs/HeroCard.prefab`.

Find the `cc.Label` `_string` currently `"HEADLESS PREFAB OK"` and change it to:

`PLAY NOW`

Do not change the prefab UUID / `.meta`.
