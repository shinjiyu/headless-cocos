# common/anim — Phase 1

Phase 1：`IAnim` + `SerialAnimQueue`  
Phase 2：`Anim.ts` + `compose.ts`（delay / call / seq / par / race / loop / forever）  
Phase 2b：`AnimEventScope` + `AnimEventSpec`（timeline event，见 `extensions/genbot/docs/anim/06-events.md`）

后续：builders、Vitest in common（见 genbot `extensions/genbot/docs/anim/`）。
