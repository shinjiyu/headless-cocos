# Headless runtime 3.8.8

Ships in this repo. `preview-mirror` and mini-packer resolve this folder automatically.
Do not install Cocos Creator.
Do not run npm install / npm ci / npm prune in this directory.
