import { Chunk } from '../chunk';
import { ChunkUID } from '../chunk-id';
export declare class ChunkIOBase {
    constructor({ chunkHomePath, }: {
        chunkHomePath: string;
    });
    serializeRecord(): unknown;
    deserializeRecord(json: unknown): void;
    protected clear(): void;
    protected _calculateChunkCodeFileName(chunkId: string): string;
    calculateChunkCodeFileRelativePath(chunkId: string): string;
    queryAllTimestamps(): Record<string, number>;
    protected _chunkHomePath: string;
    protected _build: {
        chunks: Record<ChunkUID, Chunk>;
        entries: Record<string, ChunkUID>;
    };
}
