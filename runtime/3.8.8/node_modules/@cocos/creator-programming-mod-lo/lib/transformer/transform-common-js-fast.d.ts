import { CommonJsMod, JavaScriptSource, Specifier, TransformResolver } from "../mods";
export declare class FastCommonJsMod implements CommonJsMod {
    readonly type = "commonjs";
    constructor(source: string, id: string | undefined);
    source(): {
        code: string;
        map: undefined;
    };
    module(): JavaScriptSource;
    systemjs(resolver?: TransformResolver): {
        source: JavaScriptSource;
        moduleSpecifiers: Specifier[];
    };
    private _source;
    private _id;
    private _cjsAnalyzeResult;
}
