/// <reference types="node" />
import type { RawSourceMap } from "source-map";
import type { URL } from "url";
import type { BrowsersListTargets } from "../mod-lo";
import { CommonJsMod } from "../mods/common-js";
import { EsmMod } from "../mods/esm";
import { JsonMod } from "../mods/json";
export declare type InputSourceMap = RawSourceMap | string;
export declare type TransformTargets = BrowsersListTargets;
export interface Transformer {
    /**
     * Transform an ESM module.
     */
    transform(url: URL, source: string, inputSourceMap: InputSourceMap | undefined, options: TransformOptions, disableTransform?: boolean): EsmMod;
    /**
     * Transform a JSON module into ESM representation.
     */
    transformJson(url: URL, source: string, inputSourceMap: InputSourceMap | undefined): JsonMod;
    /**
     * Transform a CommonJS module into ESM representation.
     */
    transformCommonJs(url: URL, source: string, inputSourceMap: InputSourceMap | undefined, id: string | undefined, disableTransform?: boolean): CommonJsMod;
    /**
     *
     */
    loadHelper(url: URL): EsmMod;
}
export interface TransformOptions {
    cr: boolean;
    annotate?: {
        baseName: string;
        compressedUUID: string;
        hot?: boolean;
    };
    checkObsolete: boolean;
}
export interface CircularReferenceReportOptions {
    moduleRequestFilter?: RegExp | Array<RegExp>;
    reporter: {
        moduleName: string;
        functionName: string;
    };
}
