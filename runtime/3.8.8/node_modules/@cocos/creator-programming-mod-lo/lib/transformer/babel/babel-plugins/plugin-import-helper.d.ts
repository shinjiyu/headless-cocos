import type * as babel from '@babel/core';
export default function importHelper({ helperModule, }: {
    helperModule: string;
}): ({ types }: typeof babel) => babel.PluginObj<{
    set(name: 'helperGenerator', callback: (name: string) => void): void;
    availableHelper(name: string): boolean;
    opts: {
        imports: string[];
    };
} & babel.PluginPass>;
