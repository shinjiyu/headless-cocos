import type * as babel from '@babel/core';
declare function $({ template, types }: typeof babel): babel.PluginObj<{
    filename: string;
    opts: $.Options;
}>;
declare namespace $ {
    interface Options {
        baseName: string;
        compressedUUID: string;
        importMeta?: boolean;
        hot?: boolean;
    }
}
export default $;
