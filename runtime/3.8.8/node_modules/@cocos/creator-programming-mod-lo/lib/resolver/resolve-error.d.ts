/// <reference types="node" />
import { URL } from 'url';
/**
 * Specifier 是无效的 URL、包名或包的子路径 specifier。
 */
export declare class InvalidModuleSpecifierError extends Error {
    constructor(specifier: string);
}
/**
 *  `package.json` 配置无效或包含无效的配置。
 */
export declare class InvalidPackageConfigurationError extends Error {
    constructor(packageURL: URL);
}
/**
 * 包的导入或导出为包定义了目标模块，但该目标模块的类型无效或指定了一个字符串目标。
 */
export declare class InvalidPackageTargetError extends Error {
    constructor(packageURL: URL);
}
/**
 * 指定模块所在包的导出没有定义或者不允许导出目标子路径。
 * Package exports do not define or permit a target subpath in the package for the given module.
 */
export declare class PackagePathNotExportedError extends Error {
    constructor(subpath: string, packageURL: URL);
}
/**
 * Package imports do not define the specifier.
 * 包的 `imports` 中未囊括该 specifier 的定义。
 */
export declare class PackageImportNotDefined extends Error {
    constructor(specifier: string, packageURL: URL);
}
export declare class ModuleNotFoundError extends Error {
    constructor(specifier: string, parentURL: URL);
}
export declare class UnsupportedDirectoryImportError extends Error {
    constructor();
}
/**
 * ModLo 特定：解析出的 URL 含有 “cannot-be-a-base-URL” 状态。
 */
export declare class CanNotBeBaseURLError extends Error {
    constructor(url: URL);
}
/**
 * ModLo 特定：无法从源 ESM 模块中解析目标 CommonJS 模块。
 */
export declare class CjsInteropError extends Error {
    constructor(url: URL, error: Error);
}
/**
 * ModLo 特定：CommonJS 模块必须是文件模块。
 */
export declare class CjsModuleNotFileError extends Error {
    constructor(url: URL);
}
